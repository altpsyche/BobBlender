{
  "mcpServers": {
    "bobblendermcp": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PROJECT_DIR:-.}/tools",
        "--extra",
        "mcp",
        "bob-mcp"
      ],
      "env": {
        "BOB_COMFY_DIR": "/home/siva/dev/ComfyUI"
      }
    }
  }
}
